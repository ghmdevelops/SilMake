# Senha de acesso ao painel /admin
# Defina um valor forte aqui e também na variável de ambiente
# equivalente nas configurações de build do Netlify (Site settings > Environment variables).
VITE_ADMIN_PASSWORD=troque-esta-senha
